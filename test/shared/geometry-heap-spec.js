import { assert } from 'chai';
import GeometryHeap from '../../src/shared/geometry-heap';

describe('shared/geometry-heap', () => {

  it('should...', () => {
    const heap = new GeometryHeap();
    assert(heap);
  });

});
